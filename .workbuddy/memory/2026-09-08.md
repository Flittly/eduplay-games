# 2026-09-08

## 新游戏上架：山河三消（shanhe_match3 v0.1.0）
- 新模块 `shanhe-match3/`：省级行政区三要素匹配消消乐（轮廓+简称+行政中心=同省三张才能消除）。
- 布局算法：构造式保证可通关——先造参考取牌顺序（三明治模板：两省一组，各省一对牌相邻+一张打散），再按逆序自底向上放置（后放者必在上），referenceSolvable 运行时逐步实测；greedySolvable 贪心优选（L1 77% 无脑可赢、L2 20%、L3 靠提示/撤回，重试 40 次）。教学挑战与可解性权衡见 scripts/solvability.test.cjs。
- 数据：src/data/provinces.json（34 省轮廓+简称+行政中心，轮廓与 province-puzzle 同源，已用 E:\Self\资源 2022 省级 shapefile 校验，34 条记录省级码一致）。
- 三关：L1 8 省 2-3 层 / L2 18 省 3-4 层 / L3 34 省 4-5 层；卡槽 6 格；提示×3（-5）、撤回×3（-2）、消除+10、通关+20。
- 错题档案：localStorage `shanhe_match3:mistakes:v1`，按学生存薄弱省区（卡槽未消组合+用提示的省）。
- 上架流程（已验证的管理端 API 链路）：
  1. POST /api/v1/admin/login（引导账号 admin/admin123，见 AdminBootstrapRunner）
  2. POST /api/v1/admin/games 建游戏（DRAFT）
  3. POST /api/v1/admin/games/{gameCode}/packages 上传 zip（multipart，字段 file）
  4. PATCH /api/v1/admin/games/{id}/status {"status":"ACTIVE"}
  5. POST /api/v1/admin/codes/generate {"gameCode","count"} 生成激活码
  6. E2E 已验证：兑换 /store/redeem → install → manifest → /api/v1/plugin/{token}/{gameCode}/{version}/web/index.html 全部 200
- 生成 3 个激活码：SHANHE-8U3Q-QX46（已被测试教师 e2e_teacher01 使用）、SHANHE-AH6E-M3VG、SHANHE-H2AG-E74J。
- 冒烟测试：无头 Chrome + 包装页（iframe 注入 GAME_INIT）通过；注意 file:// 下 iframe 跨域，需 HTTP 服务。agent-browser 的 Chromium 下载被网络阻断，可用系统 Chrome（C:/Program Files/Google/Chrome/Application/chrome.exe）。
- 平台商城当前游戏 product id：1=province_puzzle，2=shanhe_match3。

## 项目认知（eduplay-games 模块化游戏仓库）
- 三个游戏插件：province-puzzle（行政区拼图 v0.3.6）、province-quiz（省级行政区识别 v0.2.0）、geo-gomoku（经纬度五子棋 v0.1.4）。
- 技术栈：React 19 + TypeScript + Vite 8，`base: "./"`，构建产物 `dist/web/`（含 index.html），打包 zip 时结构为 `manifest.json + web/`。
- manifest.json 是插件契约：gameCode/name/version/type=iframe/sdkVersion=1/entry=web/index.html/backendPlugin=null/min、maxPlatformVersion。
- 与平台（E:\Self\workspace\eduplay，Java 后端 + Vite 前端）通过 postMessage 通信：
  - 游戏 → 平台：GAME_READY（gameCode+version）；GAME_COMPLETE（roundId UUID 幂等、score、correctCount、totalCount，可带 studentId/timeSeconds）；GAME_SESSION_COMPLETE（results 数组，含 mistakes）。
  - 平台 → 游戏：GAME_INIT，payload 含 gameCode/version/roster（studentId、studentName、className、studentNo），兼容旧版单学生字段。
  - source 固定 "eduplay-game" / "eduplay-platform"。
- 平台侧 GamePage.tsx 收到 GAME_READY 后 sendInit；GAME_COMPLETE 走统一积分接口 POST /api/v1/games/{gameCode}/scores（roundId 幂等）。
- 插件分发：backend/plugins/packages/{gameCode}-{version}.zip，安装到 backend/plugins/installed/{userId}/{gameCode}/{version}，前端用 GET /api/v1/plugin/{token}/{gameCode}/{version}/{entry} 以 iframe 加载。
- 测试激活码：PROVINCE-PUZZLE-2026。
- 新游戏开发要点：独立目录 + manifest.json + main.tsx 内 postMessage 通信样板（postToPlatform/notifyReady/notifyComplete/notifySessionEnd/parseRoster 可复用）。

## 商城上架修复（15:58）
- 问题：游戏商城前端列表走 `/cloud-api`（Vite 代理 → localhost:18080 云端 server/），而上架只发到了 8080 本机后端，导致商城看不到。
- 修复：已通过 18080 云端管理端（admin/admin123）完成上架：创建游戏(id=5) → 上传 v0.1.0 插件包 → 置 ACTIVE → 生成 5 个激活码。
- 云端激活码（SHANHE-R26X-Y4GP 已被 cloud_teacher02 测试占用）：SHANHE-PQAB-CNF2 / SHANHE-GEZW-VMCB / SHANHE-DHMC-KFPZ / SHANHE-FSEL-ACAN。
- 验证：云端注册教师 → 商城列表含 shanhe_match3 → 兑换 → 下载插件包 200（91KB, manifest ok）。
- 教训：**上架必须发到 18080 云端 server，不是 8080 本机 backend**；两套接口同构（/api/v1/admin/*）。

## 离线导入验证（16:10）
- 确认离线 zip 导入链路：`POST /api/v1/store/games/import-package` 解析 zip 根目录 manifest.json（gameCode/name/version/description/entry/tags）→ 自动创建/更新 game_product（无记录时新建并置 ACTIVE）→ 记录 GamePackage（版本+sha256+大小）→ 按用户写 installedVersion。
- 实测：全新教师账号导入 shanhe_match3-0.1.0.zip → 游戏中心 `/me/games` 立即显示「山河三消 v0.1.0」。gameCode 不匹配会报 MANIFEST_MISMATCH。

## 游戏封面随包走（16:33-17:20）
- 四个游戏新增 public/cover.svg（统一纸墨拼贴风、配色各异：拼图·靛蓝/识别·竹绿/五子棋·赭石/三消·朱红），manifest 增加 "cover": "web/cover.svg"，版本各 +1（puzzle 0.3.7 / quiz 0.2.1 / gomoku 0.1.5 / match3 0.1.1）。
- 平台改造（backend/ 与 server/ 双端同步）：manifest 解析 cover 字段；上传/导入时写 coverUrl=/api/v1/store/games/{code}/cover；新增公开封面接口 GET /store/games/{gameCode}/cover（直接从最新插件包 zip 流式提取，不落库）；前端 StorePage/DashboardPage 有封面显图、无封面回退 emoji，StorePage 云端列表把 /api/v1 重写为 /cloud-api。
- 四包已上传云端(18080)+本机(8080)；本机原缺 province_quiz/geo_gomoku 条目，已用 admin 创建。
- 验证：8 个封面接口全部 200 image/svg+xml；商城列表/游戏中心(/me/games)/离线 zip 导入三条链路 coverUrl 全部正确；真实接口渲染截图确认效果。
- 注意：两个后端服务目前由本会话后台进程承载（backend: java -jar eduplay-backend.jar --spring.profiles.active=local；server: java -jar eduplay-server-1.1.0.jar），会话结束后如服务停止需按此命令或 IDE 重启。
- 踩坑记录：①此环境 mvn/mvn.cmd 的 bash 调用异常，需 java -classpath plexus-classworlds 直接调 Launcher 并带 -Dmaven.multiModuleProjectDirectory；②系统命令行工具一度无 stdout，需落盘回读；③multipart 上传中途杀进程会把服务搞挂起，重启即可；④repackage 失败会留下无主清单属性的 jar，需删掉重打。

## 省区识别 logo 重做（19:40）
- 问题：问号（x=88,y=52）被右上角对勾印章（x82-104,y14-36）遮挡，构图偏乱。
- 重做：问号放大居中于竹绿省区轮廓内（白字黑描边，点题"看轮廓猜省名"），对勾印章改朱红小方章放右下角，互不遮挡。
- 发布：province-quiz v0.2.2，已上传云端(18080)+本机(8080)；两端封面接口已返回新 SVG，商城列表版本显示 0.2.2。
- 另：17:30 已修复商城 logo 过大问题（删除 styles.css 中 .store-card .game-cover 的整卡宽放大覆盖，统一 46px）。
