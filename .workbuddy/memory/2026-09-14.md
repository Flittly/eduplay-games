# 2026-09-14

## 地貌人格测试 v1.3.0：题库 15 → 20 题，且全部改为「地理情境」命题

### 需求（用户原话要点）

题目从 15 道增到 20 道；「和地理相关的题目太少了」，要**测心理但和地理有关**的题目。
两端都要更新：网页端 `E:\Self\workspace\landform-quiz-web`、游戏端 `eduplay-games/landform-quiz`。

### 先摸清结构（省了大量返工）

- 两端是**同一 App 的两份副本**（分叉点 v1.1.0，见 web 端 README）。`public/data/questions.json`、
  `landforms.json`、80 张照片、地图底图**文件级完全相同**；`src/` 只有 5 个文件不同
  （`App.tsx`/`brand.ts`/`main.tsx`/`poster.ts`/`styles.css`），差异集中在**页脚**：
  web 版是「小红书二维码 + 自动生成的网站码」，游戏端只有一行数据来源文字（无 `brand` 导入）。
  → **改题库的正确做法**：只改 web 端 `public/data/questions.json`，再 `cp` 覆盖游戏端，
  绝不要两边手改（副本不会自动同步，这是 README 里就写明的坑）。
- **题数在 UI 里没有硬编码**：进度条是 `((cursor+1)/questions.length)*100`、题号是
  `第 {cursor+1} / {questions.length} 题`、数字键提示读 `options.length`。要改的只有**文案**
  里的「15 道题」共 8 处（两端各 4 处：`src/App.tsx`×2、`src/brand.ts`、`index.html`×2、
  `README.md`、游戏端 `manifest.json` 的 description）。
- 网页端的「发布」= **WorkBuddy 站点部署**，发布目录是**快照 `site/`**（内容 = `dist/web` 的副本，
  含 `public/_headers` 与 `cover.svg`）。`vite.config.ts` 里**没有** `server.host/allowedHosts`
  ⇒ 当初不是按「Vite 项目」发布的，而是按**纯静态目录**发布的，所以继续走 `site/` 这条路。
  更新快照要**先 `rm -rf site` 再 `cp -r dist/web/. site/`** —— 否则上一版的 hash 文件名
  （本轮实测残留 `index-C__Hk90l.js`、`index-DIZ47FmM.js`）会留在目录里被一起上传。

### 题库设计：五维配平是硬约束（这次的重点）

原有五维人格框架：`thermal` 温度（冷静内敛↔炽热张扬）、`relief` 起伏（平和低调↔高远出众）、
`hardness` 质地（柔韧随和↔刚强棱角）、`dynamics` 动态（沉静恒定↔激荡多变）、
`moisture` 水意（独立自持↔丰盈亲和）。

**写在 `logic.ts` 里的归一化机制决定了配平规则**：
`ceil[d] = Σ_q max_o |delta_o[d]|`，`t = clamp(raw[d] / (ceil[d] * 0.62))` → `50 + 50t`。
所以每个维度必须满足：

1. **该维度「各题最大正值之和」≥ 0.62 × ceil**，否则回答再热情也到不了 100；
   负向同理，否则到不了 0。（本轮五维的 max正和 = 13/14/16/14/16，max负和 = 16/16/14/14/16，
   ceil 全为 16 —— 都过线。）
2. **维度覆盖要均**：20 题 × 每题 2 维 = 40 个槽位 ÷ 5 维 = **每维出现恰好 8 次**。
   我先把「题号 → 维度对」排成一张 20 行的表，让 T/R/H/D/M 各出现 8 次，再填地理情境；
   先定情境后配维度必然会偏（第一版 H 出现了 14 次、R 只有 6 次）。
3. **选项顺序会带来系统性偏差**：模拟「每题都选第 N 项」，全选第 1 项得
   `温度90/起伏85/质地75/动态75/水意100`，全选第 2 项 `温度0/起伏5/...` —— 因为多数题的
   第 1 项是「热/湿」极。这是情境题的自然结果，真实作答不受影响，但**说明里别宣称**能靠它测出
   绝对分数。若要彻底消除，需要逐题打乱选项顺序（本轮没做）。
4. 把这份规则写成脚本 **`scripts/check_questions.py`**（两端各一份）：查 JSON 结构、
   delta 键合法与 −2~2、维度出现次数、正/负可达性（≥0.62×ceil）、并打印三种极端作答的
   五维得分。**改题后必须跑它**——它就是这套题目的守门人。

20 道题的地理情境（全部命中地理关键词，实测覆盖 20/20）：气候带选居地、大河上中下游、
沙漠绿洲、自比地貌、潮汐海岸、桂林溶洞、休眠火山温泉、暴雨与河堤、等高线地图、北极圈越冬、
湿地/山间湖/高原咸水湖、新疆时差、沙尘暴、海拔四千米的高原反应、冰川磨谷、雨林、
黄河改道、地质时间里的岩石、水体自比、板块挤压抬升山脉。
（借用了「新疆与北京日落差两小时」「黄河改道」「桂林溶洞」「青藏高原」等中国地理案例。）

另外把首页介绍段改成「20 道题都从真实的地理情境出发——气候带、河流、溶洞、冰川、高原、
时区、板块……」，让「地理」这件事在第一屏就说清楚。

### 验证（Electron + 真实鼠标输入）

脚本 `.workbuddy/tmp/lqverify/shot.js`（照 `pkverify/shot.js` 改）。**6 组断言全过 / 0 失败**：

- 首页 tagline 为「20 道题」、介绍段含「地理情境」；
- **从 `data/questions.json` 实读题数 = 20**（**不要**只断言文案里的 20 —— 那等于用文案证明文案）；
- 逐题作答 20 次：题号严格 1→20 递增、每题题干非空、每题 3 选项；答完进结果页
  （实测「武夷山 81%」）；
- **地理关键词覆盖率 20/20**（量化「和地理有关」这个诉求，而不是凭感觉说"更地理了"）；
- 控制台零 error。

截图归档到 `landform-quiz-web/screenshots/v1.3.0-{home,quiz-q1,quiz-q10,result}.png`。

### 发布

- 网页端：`site/` 快照更新后走 WorkBuddy 站点部署，链接
  **https://landform-quiz.app.workbuddy.host/**（同一 app 复用，链接不变）。
- 游戏端：`python .workbuddy/tmp/publish_landform.py` → 双端 7070/17070 均
  `landform_quiz v1.3.0 ACTIVE`、cover 200、教师端商城可见；包 **6.76 MB**（80 张照片）。
  简介随 manifest 自动同步为「答 20 道题……」（118 字，仍在 ~120 字以内）。
- `check_desc.py` 原来是**硬编码 `earth_globe`/`solar_system` 白名单**，会漏掉其他游戏
  ⇒ 改成列出全部游戏（两端各 7 款），以后任何游戏的简介变化都能看到。

### 注意（并行会话）

本次收尾时发现工作区里另有一批**未提交**改动：`shanhe-match3/` 的 1200+ 行（消除记忆弹窗、
提示升级、悔棋精确回退）以及 `memory/2026-09-13.md` 追加的「山河三消 v1.0.0」段落和
`MEMORY.md` 的**拆分**（地球仪/太阳系细节移入新建的 `globe-and-solar-notes.md`）。
那是**另一个会话**的工作，完成状态未知 ⇒ **本轮没有提交它们**，只提交了 landform-quiz。
`MEMORY.md` 因此暂未改动（避免把别人的未完成改动一起 commit）。

---

## 新游戏 weather-quiz「气象要素识别」v0.1.0（已上架双端）

玩法骨架复刻 `province-quiz`（单人答题 + 双人 PK + 积分规则 + 学生排行榜 + postMessage 协议），
识别对象换成初高中教材要考的气象要素。新目录 `weather-quiz/`。

### 产出

- **100 道题**（初中 50 / 高中 50）+ **88 条要素图鉴**，分别落在
  `public/data/questions.json`（含 `levels` 的考查范围）与 `public/data/elements.json`。
  生成脚本 `scripts/build_bank.py` 带自检（id 唯一 / 候选池≥4 / 答案在池内 / 选项互不相同 /
  图片存在 / 答案落位分布），种子取题目 id 的 md5 ⇒ **构建可复现**。
- **88 张素材**（`public/assets/weather/`）：矢量 68 张（天气符号 21 + 风向风力 4 +
  气候统计图 10 + 天气系统剖面 12 + 大气受热/热力环流 10 + 大气环流 11）+ **真实照片 20 张**。
  矢量由 `scripts/svgkit.py`（气象图元库）+ `make_symbols.py` / `make_fronts.py` /
  `make_atmos.py` / `make_circulation.py` 生成；照片由 `scripts/fetch_photos.py` 按
  landform-quiz 那套过滤纪律采集（20 张全部逐格目视复核过）。
- 前端：`types.ts` / `data.ts`（运行时 fetch JSON + 重试）/ `Media.tsx`（四种素材形态）/
  `Gallery.tsx`（图鉴）/ `WeatherQuiz.tsx`（单人）/ `DualQuiz.tsx`（双人 PK）/ `main.tsx`（大厅）/ `styles.css`。
- 大厅一屏解决：选等级（初中/高中，各带 5~7 条考查范围）→ 选模式（单人 / 双人 PK / 图鉴）。
- **`DualQuiz.tsx` 沿用单一结算出口 `settleQuestion()`**，答对与"双方都答错"都走它。
  另加：**题一旦揭晓就把正确项标红**（`.pk-options button.is-answer`）—— 作废后四个按钮全 disabled，
  不标红学生根本看不出答案在哪。

### 验证（Electron + 真实鼠标输入，`.workbuddy/tmp/wqverify/shot.js`）

最终构建 **`ok=true` / 0 失败**，四组覆盖：

- **A 素材体检**：从数据 JSON 反查并逐张 `new Image()` 载入，**188/188 全部可达、0 失败**；
- **B 单人两人全流程**：10 题逐题断言「押错 → 该选项被禁用且分数不变」「答对 → 得分 = 10 −
  押错次数×2 − 提示条数×2」（实测 60/74 均逐题精确吻合）、首答正确数吻合、结算卡片分数吻合、
  排行榜 `完成 2/2`、is-done 2 行 / is-pending 0 行；
- **C 双人 PK 死锁回归**：左侧一律押 0 号、被锁定则右侧也押 0 号 ⇒ 本轮造出 **7~9 道"双方都答错"**
  的作废题，**10 题全部推进、必出结果页、无一轮卡死**（脚本里 `09-pk-stuck.png` 从未生成即是证据）；
  并断言作废题左右两侧高亮的是同一个选项、且与提示里的正确答案一致；
- **D 图鉴**：总数 88、9 个分类筛选的卡片数与数据逐项相等（21/4/10/12/10/6/5/14/6）、
  高中筛选 41、详情弹窗能开图能关。

截图 13 张归档在 `.workbuddy/tmp/wqverify/`（`screenshots/` 已被 gitignore，不进仓库）。

### 上架

`python .workbuddy/tmp/publish_weather.py` → 双端均 **`weather_quiz v0.1.0 ACTIVE`**
（本地 7070 新建 id=69、云端 17070 新建 id=8），cover 接口 `200 image/svg+xml`，
教师端商城可见。包 **1.42 MB**（95 个文件，顶层只有 `manifest.json` + `web/`，脚本已断言）。

### 本轮踩的新坑（已写进 skill `frontend-visual-verify` §9.3~§9.5 与 MEMORY.md）

1. 静态服务的 cwd 锁住目录 ⇒ `rm -rf` / `shutil.rmtree(ignore_errors=True)` 静默失败，
   旧 hash 文件残留且仍被 200 应答。必须**先停服务再清目录**，并用"旧 hash 404"取证。
   （本机 `rm -rf` 还会被 safe-delete 拦下，报 `genie-trash failed` + exit 1，一律改用 Python 删。）
2. `capturePage()` 会给出"**类名已生效、画面还是上一帧**"的图 ⇒ 曾误判新 CSS 没生效。
   样式结论改用 `getComputedStyle` 读数字下判断。
3. 回归脚本自身两处笔误各白跑一轮：`expectedFirstTry` 变量名写错（ReferenceError）、
   以及"按选项下标递增重押"会点到已禁用按钮 ⇒ 改成按 `enabledIdx` 取第一个可点的，
   否则"押错次数"会被多记，扣分断言跟着错。

---

## weather-quiz 暖锋符号修正：半圆直径必须压在锋线上

用户反馈「暖锋符号的半圆太靠上，直径应与锋线在同一水平面」。

**根因**：`scripts/svgkit.py` 的 `front_line()` 里，暖锋半圆的圆心被放在**法向 r 处**
（`ax, ay = x + px*r, y + py*r`），于是直径悬在线外 10px、只有弧顶碰到线 ——
整个半圆"骑"在线的上方。冷锋三角的底边本来就在线上，两者不一致。

**修法**：圆心落回锋线上的采样点，直径与线重合，弧沿 -p 侧鼓出（`sweep=1` 固定 ⇒
弧脊在 `圆心 + r*(uy, -ux)`）。抽成内部闭包 `half_disc(r, fill)`，暖锋 r=10、
准静止锋的暖侧标记也从「整圆骑线」改成同款半圆 r=9，与三角分列两侧的语义不变。

几何自证：`front_warm.svg` x=222 处锋线 y=196，半圆两端点 (231.767,198.144) 与
(212.232,193.856) 的中点正好是 (222.000,196.000)。

**回归脚本** `.workbuddy/tmp/wqverify/front.js`（新）：把三个符号 `<img>` 画进 canvas，
扫**锋线法线那一列**的像素，断言红色不从线的上方开始（旧写法 red[0]≈186 会被判失败）、
朝线的一侧鼓出、以及两标记之间只剩线宽。9 条断言全绿。
注意准静止锋 pts=[(58,186),(140,186),(222,186),(302,186)] 下 **i=1⇒x=140 是半圆（向上）、
i=2⇒x=222 是三角（向下）** —— 采样列搞反过一次，白跑一轮。

**顺带**：素材改完 → `npm run build`（js/css hash 不变，只 public 变了）→
`publish_weather.py` 重传，双端 `weather_quiz v0.1.0` 覆盖成功（同版本覆盖平台是允许的）。

**新坑**：Electron 截图脚本漏写 `--disable-gpu-sandbox`（只写了 `--no-sandbox`）时，
`loadURL()` 永不 resolve、脚本静默挂死且无任何输出 —— 以后一律照 `diag.js` 的完整开关列表抄。

---

## 双人 PK 补齐「选人 + 姓名展示」（weather-quiz / province-quiz 同升 v1.0.0）

**问题**（用户报的）：两个游戏的 `DualQuiz` 都**不接 roster**，大屏上只有"左侧玩家 / 右侧玩家"，
谁在答、谁答对全认不出；平台名单常有 5~6 人，而 PK 一次只能上 2 个 —— 第 3 个人起完全无从上场。

**设计**（两游戏共用一套，三阶段）：`arrange` → `battle` → `result`

- **出战安排页**：左右席各一个候选人按钮组（全部学生都在，点对面席上的人＝两边对调；
  点候场的人＝他上台、原席位的人退回候场队首），下方「候场 N 人」按次序列表。
- **对战页**：两侧标题、抢答/锁定提示、顶部计分板**全部换成姓名**。
- **结果页**：计分板「甲同学 10 : 乙同学 0」+ 胜方亮金；擂台延续按钮
  「下一位上台：丙同学」—— **负者下场不回流**，所以 N 人恰好打 N−1 场就全员上场。
- `roster.length < 2` 时自动补一位「陪练同学」（`studentId: -2`），体验模式也能试玩 PK。
- 大厅摊开「本次参与学生（N 人）」名单，老师一眼确认平台选中的是谁。

**顺手修掉的两个真问题**

1. `province-quiz` 的 `settleQuestion` 用闭包里的 `index + 1`（delay 期间一旦因 setLeftWrong
   重渲染就会读到过期值）→ 改函数式 `setIndex(current => ...)`，与 weather-quiz 对齐。
2. **CSS 特异性把右席标红吃掉了**：`.right-side .pk-options button` 是 (0,3,0)，
   压过 `.pk-options button.is-answer` 的 (0,2,0) ⇒ 双方都答错时**右席的正确答案一直是金色没标红**，
   上一轮回归没测出来。改成 `.pk-side .pk-options button.is-answer` (0,3,1) 修复（两个游戏都改）。

**验证**：`.workbuddy/tmp/pkcheck/shot.js`，参数化 `PK_PORT` / `GAME_LABEL`，两个游戏各 **25 条断言全绿**：
大厅名单、席位默认值、候场顺序、三种换人路径（上位/退回队首/对调）、对战页与计分板带姓名、
结算板带姓名与胜负、擂台延续到第 2 场（题号与比分重置）。截图 10 张归档在同目录。

**版本与上架**：两游戏 `manifest.json` / `package.json` / `src/main.tsx` 三处同升 **1.0.0**；
双端 7070 / 17070 均 ACTIVE（weather id 69/8、province id 3/3），包分别是 1.42 MB / 0.09 MB。
上架脚本 `.workbuddy/tmp/publish_province.py`（由 publish_weather.py 派生，同结构自检）。

**边界补测**：`.workbuddy/tmp/pkcheck/solo.js` —— 注入**只有 1 人**的名单，断言右席自动补
「陪练同学」、页面给出说明、无候场、对战页显示陪练名。5 条全绿。
（不测这条的话，"游戏包直接丢进浏览器体验"这条路径会静默进不去 PK。）
