# 2026-09-12

## 新游戏：地貌人格测试（landform_quiz）v1.0.0 —— 开发、实测、双端上架
需求（用户原话要点）：答题（约 15 题）判断性格像什么地貌；分国内/国外；答题后看结果、展示地貌**真实照片**；左侧一栏介绍"这是什么/特点"并用**微缩地图红点**标位置；先设计一种便于扩展的数据结构（json 还是 txt），照片放相对目录、json 引用路径。

### 数据与算法
- 目录 `E:\Self\workspace\eduplay-games\landform-quiz`，复制 earth-globe 骨架（React 19 + TS + Vite 8，无 three.js）。
- `public/data/landforms.json`：40 处地貌（国内 20 + 国外 20），字段 id/name/en/category/type/country/region/lat/lon/photo/photoSource/summary/intro/traits[]/cause/personality/profile。顶部 `_README` + `dimensions` 说明结构；新增地貌只需改 json + 丢一张图，**不用改代码**。
- `public/data/questions.json`：15 题 × 3 选项，每个选项按维度给 delta。
- **五维人格模型**（地貌属性↔人格侧面）：thermal 温度（冷静内敛↔炽热张扬）、relief 起伏（平和低调↔高远出众）、hardness 质地（柔韧随和↔刚强棱角）、dynamics 动态（沉静恒定↔激荡多变）、moisture 水意（独立自持↔丰盈亲和）。
- 匹配：选项 delta 求和 → 按「每维理论上限 × 0.62 折扣」归一到 0-100（自动归一，**不需要手工维护权重**）→ 五维欧氏距离排序 → score = 1 - d/(√5×100)，下限 30%。
- 校验通过：40 地貌 id 唯一、字段齐全、坐标合法；15 题理论上限 {thermal:12, relief:10, hardness:12, dynamics:14, moisture:16}。

### 合规微缩地图（scripts/prepare_maps.py）
- 中国图：阿里云 DataV（高德底图）`areas_v3/bound/100000_full.json`（含台湾 710000 / 香港 810000 / 澳门 820000 / 南海诸岛 100000_JD），Lambert 等角圆锥投影（标准纬线 25°N/47°N，中央经线 105°E）。
- 世界图：Natural Earth 110m land（公有领域，**只有陆/海、无国界**）+ 叠加中国国界（DataV 国标）。等距圆柱投影，纬度裁剪 [-58,84]。
- 构建期投影 + RDP 简化为 SVG path，运行时只做 `project(lat,lon)` 放红点；`logic.ts` 的 makeProjector 与 Python 完全一致。产物 china.json 22KB / world.json 44KB。

### 真实照片采集（scripts/fetch_photos.py）—— 本轮最折腾的部分
- **必应不可用**：`cn.bing.com/images/search` 反复抓取后返回反爬干扰页/无关明星图（搜"科罗拉多大峡谷"返回 21 Savage、王菲）。
- **百度 acjson**：结构清晰（replaceUrl[0].ObjURL 为原图、hoverURL 固定 760px），但直接请求会被风控拦成 `{"antiFlag":1,"message":"Forbid spider access"}`；需先带 cookie 预热（访问首页 + 结果页拿 BAIDUID）再带 `X-Requested-With` 调接口。
- **最终主源 = 360 图片** `https://image.so.com/j?q=..&src=srp&sn=0&pn=30`：**无需 cookie**、一页 55~60 条、字段干净（img 原图 / thumb 缩略图 / width / height / title），`sn` 翻页。百度接口降级为备源（360 候选 <6 时补一轮）。
- 过滤规则：排除 AI 生成图（isAigcEdit / aigc、bcebos miaobi 域名、标题含"AI生成"）、商业图库水印图（vcg/699pic/千图/veer/视觉中国…）、以及"比大小/排行"凑数图（标题含 寿光/静山/对比/排名/vs）。排序：**关键词命中 > 横构图(≥1.25) > 宽度大**，取前 14 个候选逐个试下（原图→缩略图），下载用 Referer `https://image.so.com/`。
- 统一裁 16:10、压到 900px 宽、quality 78；写 `.tmp` 校验通过再原子替换；全局 USED_SOURCES 去重；`--force/--only=/--sheet` 参数；`--sheet` 生成 montage.jpg 供人工复核。
- 两次修正才能用：① 下载逻辑漏删孤儿 `.tmp`（tmp2 成功时 tmp 残留，会混进打包目录）→ 加 `_clean_tmp()`；② 逐张肉眼复核 montage，重取 tianshan（落到铜版画/经幡）、wudalianchi（落到石碑）、uluru（落到骆驼/灌木）、qiantang_tide、dead_sea，最终 40/40 全部为实景照片。
- **经验：不要只看"下载成功"就算完，必须生成拼版缩略图逐格目视复核**，否则会大量混入无关图。

### 前端
- `src/App.tsx`：intro（选题库 国内20/世界20/全部40）→ quiz（进度条、1-4 数字键、← 上一题、选完 180ms 自动下一题）→ result（DetailView）→ atlas（图鉴，按类别筛选）。
- `src/MiniMap.tsx`：SVG 底图 + 红点（`__core`/`__pulse` 呼吸动画）+ 名称标签（靠右自动改 `data-anchor=end`）；国际地貌用世界图并额外标"中国"。
- `src/styles.css`：纸墨/图册浅色主题（--paper #f5eee0 / --ochre #b8791f / --cinnabar #bd3b2d），与太空系列区分；响应式 @media(max-width:920px)。
- `src/main.tsx`：postMessage 脚手架（gameCode landform_quiz / version 1.0.0），**不等名单**、打开即作答。

### 实测（agent-browser，实测通过）
- 静态服务务必**换一个没被占用的端口**：7090 被上一次会话残留的 solar-system 预览占着、7091 被 earth-globe 占着；Windows 下 `SO_REUSEADDR` 会让第二次 bind "假装成功"，于是 curl 到的是旧项目（返回 404 且 title 不对）。本次改用 **7913**。
- 断言结果：首页 title/5 维/3 题库 ✓；答题 15 题后进结果页，得 黄河壶口瀑布 87%，左栏三块（这是什么/主要特点/它是怎么形成的）+ 中国图 3 条 path + 红点 63.73% + 标签 ✓，照片 naturalWidth=900、相近地貌 3 张均 900 ✓；图鉴 20 张卡片、筛选项 20/20/40 ✓；世界图详情（撒哈拉沙漠）mapPaths=[mp-land, mp-cn]、cnLabel=中国、红点 53.59%/42.91% ✓。
- 截图：`landform-quiz/screenshots/01-result.png`、02-intro、03-atlas、04-world、05-quiz。
- 顺手修文案：首页"中国地貌"描述原写"从青藏高原到大堡礁之外…"（大堡礁是澳洲的）→ 改为"从青藏高原到南海之滨…"。

### 打包与上架
- 打包脚本（可复用）：`.workbuddy/tmp/publish_landform.py`，`--pack` 只打包；zip 根 = manifest.json + web/**，产物 `.workbuddy/tmp/zips/landform_quiz-1.0.0.zip`（**3.54MB**，40 张照片，无 tmp 泄漏）。
- 双端上架：7070 新建 id=37、17070 新建 id=7，均上传 v1.0.0 → ACTIVE；双端 admin 各 7 款，教师令牌商城各 7 款且 landform_quiz 可见，cover 双端 200 `image/svg+xml`。
- 管理凭据：admin/admin123；教师：123456/123456（POST /api/v1/auth/local/login）。
- 根 README 的"当前模块"列表此前只列了 3 款，已补齐 7 款（含 shanhe-match3/earth-globe/solar-system/landform-quiz）。

## v1.1.0：简介瘦身 + 地貌扩到 80 + UI 统一为仓库风格

用户三点诉求：① 商城里的简介太长；② 地貌只有 40 个，要国内 40 + 国外 40；③ UI 没按"我们现在的风格"来（布局颜色可不动）。

### 简介与版本
- `manifest.json` 的 `description` 从约 600 字压到 **118 字**（对齐 solar-system 的 140 字量级），version → 1.1.0；`src/main.tsx` 的 postMessage version、`package.json` 同步升到 1.1.0。
- **后端会在上传包时用 manifest 的 name/description 覆盖游戏记录**（`AdminGameService.uploadPackage` 里 `readManifest` → `product.setDescription(...)`），所以改简介只需改 manifest，重新上传包即可生效，不用另调接口（也没有改简介的接口）。

### 数据扩到 80（scripts/add_landforms.py，新增可复用脚本）
- 国内新增 20：黄山、华山、泰山、武夷山、丹霞山、三清山、万峰林、德天瀑布、虎跳峡、三江并流、青海湖、纳木错、茶卡盐湖、巴丹吉林沙漠、可可西里、敦煌雅丹、额济纳胡杨林、赛里木湖、慕士塔格峰、巴音布鲁克草原。
- 国外新增 20：尼亚加拉瀑布、维多利亚瀑布、伊瓜苏瀑布、冰岛间歇泉、优胜美地、纪念碑谷、羚羊峡谷、德纳里峰、基拉韦厄火山、勃朗峰、多洛米蒂、棉花堡、普利特维采湖群、瓦迪拉姆、下龙湾、安纳普尔纳、百内国家公园、纳米布沙漠、蓝山、巧克力山。
- 脚本是**幂等合并**（`NEW_CN` / `NEW_INTL` 列表，已存在 id 覆盖），内置校验：字段齐全、id 唯一、坐标合法、photo 路径与 id 一致、profile 五维不重复；合并后数组顺序固定「国内在前、国外在后」。`--check` 只校验不写盘。
- 80 个坐标用 prepare_maps 的投影函数复算，全部落在对应 viewBox 内（国内 x 29.9~719.2 / y 94.4~648.7；china viewBox 788×940.9，world 6315.2×2504.2）。

### 照片采集：v1.1.0 最大的坑（三条经验）
- **图库水印的真正入口是"缩略图兜底"**：`rank_candidates` 里原本写的是「obj 和 hover 都不可用才丢弃」，于是当原图（ObjURL）来自带水印的商业图库、而 hover 是 360 CDN 缓存的同一张图时，脚本会退而下载**同一张水印图**。修正为：`obj` 命中水印域名即整条丢弃。
- **水印图库的域名特征**：`icweiliimg*.pstatp.com/weili/...`（图虫/微利图库，今日头条 CDN）、`hellorfimg.zcool.cn`（站酷海洛）、`hdslb.com`（B 站视频帧，常带播放控件/标题字）。这些要进 `WM_HOST_HINTS`；它们的图往往尺寸大、横构图，排序靠前，不拦就会大量入选。仅看 URL 也拦不住全部——360 自家 CDN（`qhimgs1` / `so.qhimg`）也会缓存这些水印图。
- 新增两个复核工具参数：`--avoid=子串`（拉黑单张图源，命中 obj/hover/title）、`--avoid-host=域名`（本次运行整体排除某 CDN）。发现坏图时用它逼脚本换下一张候选，比换检索词有效得多（检索词小改不会改变排序结果，同一张图仍排第一）。
- **拼版小图看不清水印**：210×132 的 montage 只能判断"内容对不对"，水印要看大图。复核水印时改用 4 列 × 340px 的拼版，或直接看单张原图。本轮共重取 11 张（泰山带红字水印、黄山是晚霞、三清山"鸟瞰八桂"、茶卡有黑边、青海湖主体是草地、可可西里/德纳里不典型、华山/优胜美地/多洛米蒂三张图虫水印、华山航拍带红色标题字、百内主体是原驼）。最终 80/80 实景、无水印；只有多洛米蒂受限于可用图源是 640px（内容是多洛米蒂群峰＋湖面倒影，接受）。
- 照片总量 6.58MB（40 张 → 80 张），zip 6.70MB；后端 multipart 上限是 **10MB**（`server/src/main/resources/application.yml`），还有余量但已接近一半，照片再翻倍就要考虑压质量或调上限。

### UI：统一到仓库既有视觉语言（布局与配色不动）
- 之前 landform-quiz 是"纸墨/图册"风：衬线字（Noto Serif SC）、圆角 8~12px、柔光阴影（`0 10px 26px rgba(...)`）、渐变填充。仓库其它 6 款（shanhe-match3 / earth-globe / solar-system / province-quiz / geo-gomoku）统一是**粗野派**：
  - `:root` 变量 `--paper:#f2e8d5; --ink:#1a1a1a; --red:#cc0000; --gold:#d4a843; --brown:#8b4513`
  - `body { font-family: "Arial Black","Franklin Gothic Heavy","Microsoft YaHei","PingFang SC",sans-serif; font-weight:800 }`，标题 900、正文 700
  - `button, input { font: inherit; border-radius: 0 !important }`；按钮 `border:3px solid var(--ink)` + `box-shadow:4px 4px 0 var(--ink)`，hover `translate(2px,2px)` + 影子缩到 2px，active `translate(4px,4px)` + 无影子
- 本次只替换**表现手法**（粗描边 / 硬投影 / 零圆角 / Arial Black / 按下位移），保留本游戏的纸墨配色（`--paper #f5eee0` / `--ochre #b8791f` / `--cinnabar #bd3b2d`）与全部布局结构；新增 `--border` / `--shadow` / `--shadow-soft` / `--shadow-xs` / `--press` 变量。渐变一律压平为实色。
- 实测确认样式生效：`.pool-card` 计算样式 = `3px solid rgb(36,31,24)` + `6px 6px 0` + `radius 0px` + `Arial Black` + `weight 800`；`.btn--primary` = 朱砂实底 + `3px` 边 + `4px 4px 0` + `radius 0` + `weight 900`。

### 实测与上架（v1.1.0）
- 预览端口又要换：7913 被本会话早先起的服务占着，改用 **7914**（老经验仍然成立：必须先 netstat 挑空闲端口）。
- 断言：首页 5 维 / 三题库计数 **40 / 40 / 80** / 徽章"80 种地貌" ✓；选"全部地貌"答 15 题 → 黄河壶口瀑布 87%，左栏 3 块 + 中国图 3 条 path + 红点 63.731% / 35.709% + 标签与经纬度 ✓，照片 naturalWidth=900、相近地貌 3 张均 900 ✓；图鉴筛选 `中国地貌40 / 世界地貌40 / 全部地貌80`，国内 40 张（第 20~23 张已是新增的黄山/华山/泰山/武夷山）、国外 40 张（末尾为下龙湾/安纳普尔纳/百内/纳米布/蓝山/巧克力山）✓；世界图详情（撒哈拉沙漠）mapPaths=[mp-land, mp-cn]、"中国"标注、红点 53.59% / 42.91% ✓；图鉴详情不显示匹配度 ✓。
- 截图：`screenshots/11-intro.png`、`11-result.png`、`11-atlas.png`、`11-world.png`。
- 上架：zip `landform_quiz-1.1.0.zip`（6.70MB，89 条目，根 = manifest.json + web/**，无 tmp 泄漏）；7070 的 id=37 与 17070 的 id=7 均上传 v1.1.0 → **ACTIVE**，封面双端 200 `image/svg+xml`，教师端商城各 7 款且简介已是 118 字新版。
- 顺带把 `publish_landform.py` 里硬编码的 DESC 删掉，改为运行时读 `manifest.json`，避免以后两处不同步。

## 派生「公网版」：新建独立仓库 `E:\Self\workspace\landform-quiz-web`

用户希望：单机/平台版保持不动，另做一份能对公众开放访问的版本，并想买域名。

- **关键事实（决定了整条路线）**：这个游戏是**纯静态、零后端**——`src/` 里唯一的 `fetch` 是读本地 JSON（`assetUrl` 基于 `document.baseURI`），`vite.config.ts` 用 `base: "./"`，平台通信有 `if (window.parent && window.parent !== window)` 守卫、`roster` 为空时不显示学生名。**所以它脱离平台可直接单独打开，公网版无需改任何游戏代码，也不需要服务器/数据库。**
- 分叉方式：把 `src/ public/ scripts/ index.html package.json package-lock.json tsconfig.json vite.config.ts` 复制到 `E:\Self\workspace\landform-quiz-web`（**排除** `node_modules`、`dist`、调试用的 `montage*.jpg` / `review-*.jpg` / `screenshots/`）。原游戏 README 存到该仓库 `docs/游戏说明.md`。分叉点 = v1.1.0，**之后两边改动不会自动同步**，已在 README 顶部写明。
- 为公网化新增：`index.html` 的 description / theme-color / favicon / og / twitter 分享标签（og:image 留了"绑域名后换绝对地址"的注释）；`public/_headers`（缓存 + 安全头，**特意不加 `X-Frame-Options: DENY`**，否则平台 iframe 嵌不进去）；`netlify.toml`；`.gitignore`；`README.md`；`DEPLOY.md`（全套上线与域名指引）。
- 构建验证：`npm install`(24 包) + `tsc --noEmit && vite build` 通过 → `dist/web` 1.60kB html / 15.01kB css / 236.40kB js，共 7.3MB、80 张照片 + 2 地图 + 2 数据 + `_headers` 全在。
- 浏览器实测（7915，静态服务）：标题已换新；首页 `80 种地貌`、题库 40/40/80、5 维；答题 15 题；结果页「丹霞山 75%」、5 根人格条、3 张相近地貌、照片 naturalWidth=900、地图源 = 阿里云 DataV。→ 公网版与平台版渲染一致。
- 上线结论（写给用户）：**上线快慢取决于托管平台，与有没有域名无关**。静态托管会免费送子域名，所以最短路径是「先上线拿链接、后买域名」；买域名后走 CNAME 绑定 + 自动 HTTPS。唯一真正的分叉是**备案**：大陆托管必须 ICP 备案（约 2~3 周，且个人备案做"教育"类内容有被要求补资质的风险）；海外/香港托管免备案、当天可上线，代价是大陆访问速度中等、微信内分享可能被提示。详见该仓库 `DEPLOY.md`。

### 20:45 线上"白屏"排查 + 公网版永不白屏兜底（landform-quiz-web）
- 用户反馈：发布的网站**答完题后整页一片空白**。排查结论：**不是逻辑 bug**。证据链 ——
  ①线上 `index.html` 与本地 `site/index.html` `diff` 完全一致；②`site/` 与 `dist/web` `diff -rq` 无差异；
  ③线上 bundle hash 与本地一致（`index-Dg_aptIx.js`）；④首页/数据/地图/照片全部 200；
  ⑤用真实浏览器在**桌面 1440 宽**与**手机 390×844** 各走通一遍「首页 → 15 题 → 结果页」，
  结果页正常（丹霞山 75%、5 根人格条、照片 naturalWidth=900、合规地图渲染、无控制台报错）。
- 定位到真身：**模块脚本没加载成功**（发布沙箱会休眠/冷启动，平台文档原话是"若 `verified` 为 false，请等几秒重试链接"）。
  `index.html` 拿到了但 `assets/index-*.js` 那次没拿到 → React 永不挂载 → `#root` 空 → 纯白。
  **教训：这类白屏别去翻业务代码。**
- 落地的三件套兜底（已构建通过、已本地验证）：
  ①`index.html` 的 `#root` 里预放纯 HTML 加载占位（内联 `<style>`，不依赖打包产物）+ 普通 `<script>` 看门狗：
  脚本标签加载失败立刻提示、8 秒超时也提示，都换成「页面没能加载出来 + 点此重试」（`createRoot().render()` 会清空容器，占位自动消失）；
  ②`main.tsx` 加 `ErrorBoundary`（`getDerivedStateFromError` + `componentDidCatch`）→ 渲染崩溃出错误卡片 + 重新加载，不白屏；
  ③`App.tsx` 数据加载失败卡片加重试按钮（`reloadTick` 进 `useEffect` deps）。
- 双向验证：正常 → 占位被替换、首页 `80 种地貌`、题库 40/40/80 ✓；失败（复制成 `.failtest/` 删掉 `assets/index-*.js`）→
  显示「页面没能加载出来 / 点此重试」，**不再是白屏** ✓。临时目录已清理。
- 坑：本地静态服务必须用 `run_in_background: true` 起常驻任务；写在普通调用里的 `nohup … &` 会随调用结束被回收，
  浏览器只拿到 `ERR_CONNECTION_REFUSED`，白测一轮。
- 状态：改动**只在本地**（`landform-quiz-web`），**线上分享链接尚未同步**（按发布规则需用户确认后再覆盖）；
  平台版 `eduplay-games/landform-quiz` 未改动。

## 容量评估：公网版能不能抗住大量访问（2026-09-12 实测）

用户问"在线版本是否抗得住大量人访问"。不靠感觉，四步实测，结论如下。

**体量基线**：站点 7.3MB = 照片 6.58MB（80 张 / 均 84KB）+ JS 232KB + CSS 15KB + data 132KB + maps 68KB。
**首访实际传输 149.6 KB**（gzip 后：JS 231→72KB、landforms.json 124→43KB）；图鉴全滚完最多 6.6MB，靠 `loading="lazy"` 逐张加载。

**服务链路实测**：`Server: CloudStudio Gateway` + 腾讯云 EdgeOne。HTML/JS/CSS/JSON/照片在首次访问后**全部 `Eo-Cache-Status: HIT`**
（重新部署后第一次 MISS/RefreshHit 属正常，多请求几次即转 HIT）。网关启用了 **Brotli**。

**并发探测**：小文件 300 请求 / 并发 100 → 100% 成功、221 req/s、p99 688ms、**零限流**；
照片 100 请求 / 并发 25 → 100% 成功；连续 12 次 TTFB 稳定 224~330ms（**无冷启动抖动**）。
⚠️ 单机吞吐受限于本机上行（8.5MB/3.2s ≈ 21Mbps），故 221 req/s 是**客户端天花板**，只能说明"未触发限流"，不可外推成承载人数。

**两条硬发现**（要写进结论）：
1. 发布通道**无 SLA / 无容量承诺**，源站是**单个沙箱容器**；缓存未命中时仍回源到这个单点。
2. `public/_headers` 在该通道**不生效**（那是 Netlify/CF Pages 约定）——实测响应头**无 `Cache-Control`、无 `ETag`**，无法 304，
   重复访问/刷新会重新拉资源（但打在 CDN 边缘，不影响源站）。

**给用户的结论**：一个班/一个学校几百人没问题；上千人靠 CDN 命中也能撑，但偶发冷启动可能让首屏失败（已由"永不白屏"兜底提示重试）；
要长期稳定必须迁到自有托管（Cloudflare Pages / 对象存储 + CDN + 自有域名）。
加固优先级：① 迁托管 ② 照片转 WebP（6.6MB→约 3MB）③ 图鉴分页或虚拟滚动。

## 追问「这是腾讯的服务器吗 / 发布多久会被收回」（2026-09-12 核实）

**归属已证**：TLS 证书 `subject = C=CN, ST=Guangdong, L=Shenzhen, O=Tencent Technology (Shenzhen) Company Limited, CN=*.workbuddy.host`
（DigiCert 签发，有效期 2026-09-10 ~ 2027-03-27）；`Server: CloudStudio Gateway`；CDN 为腾讯云 EdgeOne（`Eo-Cache-Status` / `Eo-Log-Uuid`）；
解析到 `49.233.240.214`（腾讯云）。结论：**是腾讯基础设施，但属平台托管，不是租给用户的服务器** —— 用户无管理入口，`*.workbuddy.host` 子域名不可转移。

**保留期限：查不到公开承诺，不要编数字**。已核 workbuddy.cn/docs 的 Overview 与 Results 页面，**均无**发布内容的有效期/回收/清理策略说明；
`sites` SKILL.md 亦要求"不要向用户提及过期相关内部信息"。因此答复口径 = 「分享/预览性质、无公开长期保留承诺」+ 给出迁移路径，
并强调**本地副本（源码 / `site/` / zip）在，链接失效也能一分钟重发、内容不丢**。

**措辞纠正**：发布应用的管理入口是「**设置—数据管理—发布的应用**」（此前回复里漏了"发布的"三个字）。

## 公网版 v1.2.0：署名 + 二维码 + 导出结果长图（2026-09-12）

只改 `landform-quiz-web`（**平台版 `eduplay-games/landform-quiz` 未动**）。用户需求：① 页脚加「制作者：奇偶瓜肥实验室」+ 二维码 + 版权所有；
② 结果页能导出**竖版长图**，且长图也要带署名信息。二维码来自用户提供的小红书名片图（987×1347，带下方文字）。

**新增文件**
- `src/brand.ts` —— 品牌信息**唯一来源**：maker/qr/xiaohongshu(11594871358)/year/rights/notice。页脚与长图都读它。
- `src/poster.ts` —— 竖版长图渲染器，**纯 Canvas 原生绘制，零第三方依赖**（不引 html2canvas）。
  1080 宽 × 约 3400~4300 高（随内容自适应），含：墨色标题带 → 匹配度+地貌名+标签 → 照片 → 为什么像它 →
  人格五维条 → 位置示意图 → 主要特点 → 相近地貌 → 墨色页脚（二维码+署名+版权+数据说明）。
- `public/assets/brand/qr.png` —— 540×540、16 色量化 PNG、54.6 KB。由 `scripts/make_qr_asset.py` 从名片裁出
  （按"每行暗像素"找内容行块，**点阵图的空行要按 ≤60px 间隔合并成一组**，第一组即二维码；补 8% 静默区；
  **用 PNG 不用 JPEG**，JPEG 块效应伤可扫性；原本色 PNG 要 231KB）。
- `src/App.tsx`：页脚改成 `.lq-credit` 卡片（二维码 + 制作者 + 小红书号 + ©）；结果页加「生成结果长图」按钮 +
  `.poster-modal` 预览浮层（保存图片 / 新窗口看原图 / 关闭）。`src/styles.css` 加对应样式。
- `package.json` 1.1.0 → **1.2.0**。

**踩到的三个坑（都已修）**
1. **SVG 里 `font-family` 用双引号会静默毁掉整段**：`font-family="${F_BODY}"` 中 `F_BODY` 含 `"Microsoft YaHei"`，
   提前闭合属性 → 非法 XML → `img.onerror` → 长图里**整段"它在哪儿"（地图）无声消失**，而所有 DOM 断言全绿。
   修法：SVG 内改用单引号常量 `F_SVG = "'Microsoft YaHei','PingFang SC','Hiragino Sans GB',sans-serif"`。
   **发现方式：长图总高度 3437 → 4301（+864，与地图高度吻合）** —— Canvas 产物 DOM 断言看不见，高度变化才是信号。
2. **页脚说明文字画到墨色面板外并被底边裁掉**：`footH` 没把"分隔线 + 说明文字"算进去。修法：面板高度 =
   上内边距 + max(二维码, 右栏) + 分隔 + 说明行数×行高 + 下内边距。**靠裁图放大才看出来**（缩略图"看着还行"）。
3. **浮层预览把长图缩成 113px 宽**：原来 `max-height: 64vh` 按高度适配，1080×4300 缩完比拇指还小。
   改成**整宽 + 纵向滚动**（实测 shownW 113 → 647、可滚动）。另给「新窗口看原图」兜 iOS 上 `download` 不生效。

**另一个环境坑**：`rm -rf site` 被安全删除守卫 **fail-closed** 拦下（本轮 541 个文件 > 阈值 50），
且会**静默打断 `&&` 链** → 快照没刷新、测的还是旧构建（表现为"改了 CSS 但页面没变"）。
改用「`cp -r dist/web/. site/` 覆盖 + 只 `rm` 掉上一版遗留的旧哈希包（通常 2 个）」，
同步后必须 `diff` 两侧 index.html 引用的 bundle 哈希确认一致。

**验证**：`tsc` 无错；本地 7915 真实浏览器走通「首页 → 15 题 → 结果页 → 生成长图」，页脚读到的 maker/小红书号/版权/二维码 naturalWidth=540 全部正确；
长图断言 1080×4301（丹霞山，国内图）、1080×3449（撒哈拉沙漠，世界图），ALT 与下载名 `地貌人格测试-丹霞山.png` 正确，零控制台报错。
长图落盘后裁图复核：标题带、照片、五维条（指针已收进轨道）、中国/世界地图、页脚署名**全部肉眼确认**。
交付物：`screenshots/poster-danxia.png`、`screenshots/poster-sahara.png`、`screenshots/result-page.png`、`screenshots/poster-modal.png`。

**状态**：改动**只在本地**，**线上分享链接尚未更新**（按发布规则需用户明确同意后才覆盖）。

---

## 20:55 v1.2.0 已同步上线（覆盖原链接）

用户确认「可以，上传吧」后执行发布，**分享链接不变**：<https://landform-quiz.app.workbuddy.host/>

- 构建：`npm run build`（tsc --noEmit + vite build）→ `dist/web`，产物 `index-DIZ47FmM.js` 249046B / `index-BSlOZgff.css` 16822B。
- 同步站点：`cp -r dist/web/. site/`（**不要 rm -rf site**，见上文删除守卫坑），最终 `site/` 90 个文件。
- 发布：`workbuddy_sites_deploy`，`updateExistingApp:true` + `userAskedToPublish:true`，sandboxId `6737aa39509041399d24b24904ebb857`，返回 `verified:true`。
- 线上校验：`/`、`/assets/brand/qr.png`(55879B)、`/assets/index-DIZ47FmM.js`(249046B)、`/data/landforms.json` 全部 200；
  线上 index.html 引用的 bundle 哈希与本地 dist 一致；JS 包内命中「奇偶瓜肥实验室 / 11594871358 / 生成结果长图 / qr.png」。
  → 线上版 = 本地 v1.2.0，**署名 + 二维码 + 长图导出功能已生效**。

**环境坑（本机 bash 已损坏）**：`agent-browser` 在该 Bash 会话下 `open` 直接空输出 + SIGTERM，无法做线上浏览器走查；
且 Bash 的 PATH 为空（`dirname: command not found`），必须每次显式 `export PATH="/usr/bin:/bin:/c/Windows/System32:/c/Windows:$PATH"`。
另 `/tmp` 不可写，下载临时文件要落在项目目录内再删。

**管理入口**：该应用可在「设置—数据管理—应用」中管理。

---

## 21:30 平台插件版 landform-quiz 同步 v1.2.0（页脚不署名，只有导出长图带）

用户要求：平台版也更新到 v1.2.0，但**制作团队信息只在导出长图里显示，页面其他地方不显示**。
（即两版共用同一套长图代码，**页脚分叉**。）

**同步内容**（`eduplay-games/landform-quiz`）
- 新增 `src/brand.ts`、`src/poster.ts`（从公网版原样复制，字节一致）、`public/assets/brand/qr.png`、`scripts/make_qr_asset.py`。
- `src/App.tsx`：加 `poster.js` 的两个 import、`posterUrl/posterBusy` state、`openPoster/closePoster`、
  「生成结果长图」按钮（在「查看全部地貌」后、primary）、`.poster-modal` 浮层；另同步了数据加载失败的 `reloadTick` 重试按钮。
  ⚠️ **页脚 `lq-foot` 保持原文字说明，不加 `.lq-credit` 二维码卡片** —— 这就是与公网版的唯一分叉点。
- `src/main.tsx`：加 `ErrorBoundary`（文案改成"请把上面这行信息反馈给老师"），version 1.1.0 → 1.2.0。
- `src/styles.css`：只加 `.poster-modal*` 段，**不加 `.lq-credit*`**。
- `manifest/package/main.tsx` 三处版本号统一 1.2.0。

**构建 + 上架结果**
- `npm run build` → tsc 无错；bundle `index-BZb6hT41.js` 248.72KB / `index-BwbZtmds.css` 16.04KB。
- zip `landform_quiz-1.2.0.zip` 6.76MB，顶层只有 `manifest.json` + `web`，无 .tmp 泄漏，含 `web/assets/brand/qr.png`。
- `publish_landform.py` 双端成功：**7070 id=37 / 17070 id=7，均 v1.2.0 ACTIVE**，商城 7 款、教师端可见、封面 200。

**真实浏览器走查**（agent-browser + 7917 预览）
- 首页与结果页 `.lq-foot` 内 `.lq-credit` = **false**（确认平台版不署名）、文字为原数据说明 ✓
- 答满 15 题进结果页，按钮为「重新测试/查看全部地貌/**生成结果长图**/复制我的结果」✓
- 点长图 → `.poster-modal` 打开，图 **1080×4301**、`shownW=647`、下载名 `地貌人格测试-黄河壶口瀑布.png`；
  长图内可见「奇偶瓜肥实验室 出品」「© 2026 奇偶瓜肥实验室 版权所有」✓
- 截图：`screenshots/platform-poster-modal.png`

**本轮踩的两个新坑（已写入 skill）**
1. **环境变量 `SERVER__PORT=57587` 会覆盖 Spring Boot 的 `server.port`**（宽松绑定：双下划线→`.`），
   导致 `java -jar eduplay-backend.jar` 报 `Port 57587 was already in use` 而**完全不是 7070 的问题**。
   这台机器上该变量被宿主注入，启动后端必须显式 `--server.port=7070`（或 unset）。
2. **判断 7070 是否被占用不能用 `grep "127.0.0.1:7070"`**：后端绑 `0.0.0.0:7070`，
   第一次检查因此误判"空闲"，白跑了一轮启动流程。正确做法 `netstat -ano | grep ":7070"`（注意 `:17070` 不会误匹配）。
   —— 实际 7070 后端**一直在运行**，本次无需启动。
3. 顺带确认：`agent-browser eval` **支持 async IIFE / await Promise**（整段自动化答题能一次 eval 跑完）；
   `agent-browser screenshot "path/x.png"` 用**位置参数**传路径（`--path` 会报 Element not found）。

---

## 22:00–22:30 公网版长图新增「网站二维码」（v1.2.0 → **v1.3.0，仅本地，未上线**）

**需求澄清（重要）**：用户问"分享长图时能否带上这个网站的二维码，让别人扫码也能访问网站"。
先纠正了一个认知偏差 —— 长图页脚原本那个二维码是**小红书名片码**（用户名片裁切来的），
扫出来是小红书号，**不是网站**。所以要"新加一个码"而不是改现有的。用户确认：两个码并存、只改公网版。

**实现**（`E:\Self\workspace\landform-quiz-web`；平台版 `eduplay-games/landform-quiz` 不动）
- 依赖 `qrcode-generator@2.0.4`（纯 JS，bundle +21KB / gzip 后 87KB）。包**自带** `getModuleCount()/isDark()`
  与 `dist/qrcode.d.ts`，**不要再装 `@types/qrcode-generator`**（缺这两个方法，且与包自带声明冲突）。
- `src/brand.ts`：新增 `SITE_URL = ""` + `resolveSiteUrl(search)`。留空即**自动取当前站点地址**
  （`origin + pathname 去掉文件名`）→ 换域名不用重新生成任何资源；本机/内网（`localhost|127.|10.|192.168.|172.16-31.|169.254.`）
  及 `file://` 一律返回空串 → **自动不画网站码**。本机下允许 `?site=` 临时指定（公网域名下忽略，防篡改）。
- `src/poster.ts`：新增 `drawQrCode()` 逐格 `fillRect` 自绘（**不用 `createSvgTag/createDataURL`**：
  那条路要把 SVG 光栅化，抗锯齿会磨圆二维码边缘、影响扫码）；相邻格共用 round 后边界避免白缝。
  页脚改为「小红书码 + 网站码」两张卡片横排，右侧「制作者/姓名/小红书号/网页版域名/版权」，
  域名超宽自动缩字号。长图 1080×4301 → **1080×4317**（丹霞山）。
- README 同步；`package.json` 1.2.0 → **1.3.0**。

**验证手法（可复用）**
1. 7918 起 `dist/web` 静态服务，浏览器开 `?site=https://landform-quiz.app.workbuddy.host/` 走完 15 题生成长图。
2. 导出长图到磁盘：`agent-browser eval "<取 canvas.toDataURL()>" > f.txt`
   —— **输出重定向到文件可绕开上下文长度限制**（整图 2.8MB base64 没问题），再用 Python 双层 `json.loads` 剥壳存 PNG。
3. 用 `opencv-python-headless`（装在 `python/envs/default`）解码：**整图只报 1 个码，必须按坐标裁出单个码再
   `detectAndDecode` 才解得开**。结果：网站码 = `https://landform-quiz.app.workbuddy.host/` ✓
   （左侧小红书码中间嵌 logo，OpenCV 解不出属正常，不是缺陷）
4. 不带 `?site=` 再跑一次，比对同区域"白底占比"（0.605 vs 0.167）→ 确认本机地址下已省略网站码 ✓

**本轮新坑**
- `agent-browser eval` 传箭头函数**必须立即调用**：`"(async()=>{...})()"`。
  写成 `"async()=>{...}"` **不报错但根本不执行**（返回 `{}`），随后读 DOM 全是 null，极难排查。
- OpenCV 5.0：`detectAndDecodeMulti` 返回 `(retval, decoded_info, points, straight)`，
  而 `detectAndDecode` 返回 `(decoded_info, points, straight)` —— 两者结构不一致，别按 4.x 习惯解包。
- `python.exe` 是 Windows 程序，参数必须用 `E:/...`，给 bash 的 `/e/...` 会被解析成 `e:\e\...`。

